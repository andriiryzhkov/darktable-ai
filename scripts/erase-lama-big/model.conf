MODEL_ID="erase-lama-big"
# Pre-converted ONNX model — no repo clone or conversion needed.
REPO_URL=""
REPO_DIR=""
REPO_INSTALL_CMD=""
REPO_HAS_REQUIREMENTS=false
CHECKPOINT_URLS=()
CHECKPOINT_PATHS=()
ONNX_URLS=("https://huggingface.co/Carve/LaMa-ONNX/resolve/main/lama_fp32.onnx")
ONNX_PATHS=("models/erase-lama-big/model.onnx")

demo_args() {
    case "$1" in
        example_01) echo "--mask 0.30,0.15,0.75,0.85" ;;
        example_02) echo "--mask 0.40,0.35,0.75,0.80" ;;
        example_03) echo "--mask 0.10,0.40,0.45,0.95" ;;
    esac
}
